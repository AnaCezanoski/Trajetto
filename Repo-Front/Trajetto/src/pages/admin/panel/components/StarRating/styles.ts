import { StyleSheet } from 'react-native';
import { adminAccent } from '@/src/theme';

export const styles = StyleSheet.create({
  stars: { fontSize: 13, color: adminAccent.amber, marginTop: 2 },
});
